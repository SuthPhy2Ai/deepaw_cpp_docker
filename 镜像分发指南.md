# DeePAW C++ 推理引擎 - 镜像分发指南

## 概述

本文档说明如何导出、传输和加载 `deepaw-cpp:v1` 容器镜像。

## 镜像信息

- **镜像名称**: deepaw-cpp:v1（最终版本，含欢迎界面）
- **基础镜像**: nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04
- **Python版本**: 3.12
- **预计大小**: 压缩后 ~6.1 GB

## 导出镜像

```bash
# 保存并压缩（一步完成）
podman save deepaw-cpp:v1 | gzip > deepaw-cpp-v1.tar.gz

# 查看文件大小
ls -lh deepaw-cpp-v1.tar.gz

# 生成校验和（用于验证传输完整性）
sha256sum deepaw-cpp-v1.tar.gz > deepaw-cpp-v1.tar.gz.sha256
```

## 在其他机器上加载

```bash
# 加载镜像
gunzip -c deepaw-cpp-v1.tar.gz | podman load

# 验证
podman images | grep deepaw-cpp

# 验证校验和
sha256sum -c deepaw-cpp-v1.tar.gz.sha256
```

## 传输方式

### 方案A：网络传输

```bash
# scp
scp deepaw-cpp-v1.tar.gz user@remote-host:/path/to/destination/

# rsync（支持断点续传，推荐大文件）
rsync -avz --progress deepaw-cpp-v1.tar.gz user@remote-host:/path/to/destination/
```

### 方案B：分割大文件

```bash
# 分割为 1GB 的小文件
split -b 1G deepaw-cpp-v1.tar.gz deepaw-cpp-v1.tar.gz.part-

# 传输后合并
cat deepaw-cpp-v1.tar.gz.part-* > deepaw-cpp-v1.tar.gz
```

### 方案C：推送到镜像仓库

```bash
# 打标签
podman tag deepaw-cpp:v1 your-registry.com/deepaw-cpp:v1

# 登录并推送
podman login your-registry.com
podman push your-registry.com/deepaw-cpp:v1

# 其他用户拉取
podman pull your-registry.com/deepaw-cpp:v1
```

## 接收方使用步骤

```bash
# 1. 加载镜像
gunzip -c deepaw-cpp-v1.tar.gz | podman load

# 2. 验证
podman images | grep deepaw-cpp

# 3. 复制 start.sh 到工作目录，然后启动
./start.sh
```

接收方机器需要：
- Linux x86_64
- Podman 或 Docker
- 至少 10 GB 磁盘空间
- NVIDIA GPU + nvidia-container-toolkit（GPU 推理必需）

```bash
# 检查 GPU 支持是否正常
nvidia-smi
```

## 常见问题

### 镜像在其他机器上无法使用 GPU？

1. 确认有 NVIDIA GPU
2. 确认安装了 nvidia-container-toolkit
3. 注意：CDI 方式（`--device nvidia.com/gpu=all`）在某些系统上不工作，需要手动挂载设备（参考 README.md 中的手动运行命令）
4. 驱动版本号需要匹配宿主机实际版本

### 如何更新镜像？

```bash
# 重新构建
./build.sh

# 打新版本标签
podman tag deepaw-cpp:latest deepaw-cpp:v1.1

# 导出
podman save deepaw-cpp:v1.1 | gzip > deepaw-cpp-v1.1.tar.gz
```

### 如何清理旧镜像？

```bash
podman images                  # 查看所有镜像
podman rmi deepaw-cpp:latest   # 删除特定镜像
podman image prune -a          # 清理未使用的镜像
```
